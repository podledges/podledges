name: profile-assets

on:
  schedule:
    - cron: "17 */8 * * *" # Refresh three times per day.
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: profile-assets-${{ github.ref }}
  cancel-in-progress: true

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate generator
        run: python3 -m unittest discover -s tests -v

      - name: Require private-repository activity token
        env:
          GH_TOKEN: ${{ secrets.WAVEFORM_TOKEN }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "::error::WAVEFORM_TOKEN is required for accurate private-inclusive GitHub contribution statistics. Configure a token owned by podledges with GraphQL and private-repository access."
            exit 1
          fi

      # Calendar publication is independent of the waveform's REST cross-check.
      # A later waveform failure remains visible but cannot freeze the new pair.
      - name: Render current contribution panels
        env:
          GH_TOKEN: ${{ secrets.WAVEFORM_TOKEN }}
          GH_USER: podledges
        run: python3 scripts/waveform.py --contributions-only

      - name: Commit refreshed contribution panels
        run: |
          git config user.name "profile-assets[bot]"
          git config user.email "actions@github.com"
          git add assets/podle-reactor.svg assets/prism-orbit-streak.svg assets/prism-orbit-bay.svg assets/profile-contributions.json docs/reactor.html
          git diff --cached --quiet && exit 0
          git commit -m "🎚 refresh contribution calendar [skip ci]"
          git pull --rebase origin "$GITHUB_REF_NAME"
          git push origin "HEAD:$GITHUB_REF_NAME"

      - name: Render current profile assets
        env:
          GH_TOKEN: ${{ secrets.WAVEFORM_TOKEN }}
          GH_USER: podledges
        run: python3 scripts/waveform.py

      - name: Commit refreshed assets
        run: |
          git config user.name "profile-assets[bot]"
          git config user.email "actions@github.com"
          git add assets/podle-reactor.svg assets/prism-orbit-streak.svg assets/prism-orbit-bay.svg assets/profile-contributions.json assets/waveform.svg docs/reactor.html
          git diff --cached --quiet && exit 0
          git commit -m "🎚 refresh profile activity [skip ci]"
          git pull --rebase origin "$GITHUB_REF_NAME"
          git push origin "HEAD:$GITHUB_REF_NAME"
